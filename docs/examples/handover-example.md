# Отчёт

**Хост:** `lab`

**Дата:** `2026-07-15`

**Всего запросов:** 830

## Топ URL по ошибкам 500

- `/api/checkout` — 308 запросов
- `/api/payment` — 21 запросов
- `/api/profile` — 12 запросов
- `/api/cart/add` — 10 запросов
- `/api/orders` — 9 запросов

## Топ-3 IP по запросам

- `185.220.101.1` — 308 запросов
- `45.33.32.156` — 153 запроса
- `203.0.113.5` — 72 запроса

## Сеть и связность

- Интерфейс: `eth0` — `10.10.115.94/32`
- Default gateway: `169.254.1.1`
- Локальная L3-проверка `ping backend`: **OK** — `backend` резолвится в `127.0.0.1`
- Связность именно с default gateway в сохранённом выводе миссии не зафиксирована
- DNS `dostavka-eda.local`: `127.0.0.1`
- TCP `localhost:80`: **succeeded**
- HTTP `http://localhost`: **200**

> Обновлённый `scripts/generate-handover.sh` проверяет L3-связность непосредственно с default gateway при каждом запуске.

## TL;DR

Атака с IP 185.220.101.1: 308 запросов, в основном 500 на URL /api/checkout. Локальный web-сервис отвечает HTTP 200; DNS-резолвинг `dostavka-eda.local` работает.
