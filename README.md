# Server Handover Report

Учебный DevOps-проект, в котором небольшое Flask-приложение постепенно превращается в полноценный инфраструктурный стенд.

Проект используется для практики управления приложением, автоматизации локальных операций, настройки reverse proxy, балансировки нагрузки и дальнейшего перехода к контейнеризации, CI/CD и оркестрации.

## О проекте

Приложение может запускаться в нескольких экземплярах на разных портах. Каждый экземпляр получает собственный идентификатор, лог и PID-файл.

Перед backend-серверами расположен Nginx, который выступает единой точкой входа и распределяет запросы между экземплярами приложения по алгоритму round-robin.

Текущая схема:

```text
                          ┌────────────────────┐
                          │     backend-1      │
                          │  127.0.0.1:3000    │
                          └─────────▲──────────┘
                                    │
Client ──► devops-pet.local:8080 ──► Nginx
                                    │
                          ┌─────────▼──────────┐
                          │     backend-2      │
                          │  127.0.0.1:3001    │
                          └────────────────────┘
```

Если один экземпляр приложения становится недоступен, Nginx продолжает направлять запросы на работающий backend.

## Реализовано

- Flask-приложение с HTML-страницей и API;
- конфигурация через переменные окружения;
- запуск нескольких экземпляров приложения;
- отдельные логи и PID-файлы для каждого экземпляра;
- автоматическое обнаружение запущенных backend-процессов;
- проверка состояния через health check;
- безопасная остановка экземпляров;
- Nginx reverse proxy;
- локальное доменное имя `devops-pet.local`;
- round-robin балансировка нагрузки;
- обработка отказа одного backend;
- endpoint с метриками Prometheus.

## API

Приложение предоставляет несколько маршрутов:

| Маршрут | Назначение |
|---|---|
| `/` | Главная HTML-страница |
| `/api/health` | Проверка состояния приложения |
| `/api/info` | Информация о приложении и экземпляре |
| `/metrics` | Метрики в формате Prometheus |

Пример ответа `/api/info`:

```json
{
  "app": "DevOps Pet Service",
  "hostname": "ubuntu",
  "instance": "backend-1",
  "version": "0.1.0"
}
```

Поле `instance` позволяет увидеть, какой backend обработал запрос, и проверить работу балансировщика.

## Структура проекта

```text
server-handover-report/
├── app/
│   ├── main.py
│   ├── requirements.txt
│   └── templates/
│       └── index.html
├── infra/
│   └── nginx/
│       ├── devops-pet.conf
│       └── check-curl.sh
├── scripts/
│   ├── setup-app.sh
│   ├── start-instances.sh
│   ├── discover-app.sh
│   └── stop-instances.sh
├── .gitignore
└── README.md
```

## Управление приложением

### Подготовка окружения

```bash
./scripts/setup-app.sh
```

Скрипт создаёт виртуальное окружение и устанавливает зависимости приложения.

### Запуск backend

```bash
./scripts/start-instances.sh \
  3000 backend-1 \
  3001 backend-2
```

Аргументы передаются парами:

```text
<порт> <имя экземпляра>
```

Для каждого экземпляра создаются:

```text
logs/<instance>.log
logs/<instance>.pid
```

### Проверка состояния

```bash
./scripts/discover-app.sh
```

Скрипт анализирует слушающие TCP-сокеты, находит процессы приложения и проверяет endpoint `/api/health`.

В результате выводятся:

- PID процесса;
- порт;
- команда запуска;
- HTTP-код;
- состояние экземпляра.

### Остановка backend

```bash
./scripts/stop-instances.sh backend-1 backend-2
```

Перед остановкой скрипт проверяет, что PID действительно принадлежит процессу `app/main.py`.

## Nginx

Конфигурация Nginx находится в:

```text
infra/nginx/devops-pet.conf
```

В конфигурации определена группа upstream:

```nginx
upstream devops_backend {
    server 127.0.0.1:3000;
    server 127.0.0.1:3001;
}
```

Nginx принимает запросы на:

```text
http://devops-pet.local:8080
```

и распределяет их между двумя backend-серверами.

Для локального разрешения имени используется запись:

```text
127.0.0.1 devops-pet.local
```

Проверить работу балансировки можно скриптом:

```bash
./infra/nginx/check-curl.sh
```

Последовательные ответы `/api/info` показывают, какой экземпляр обработал каждый запрос.

## Отказоустойчивость

При работе двух экземпляров запросы распределяются между `backend-1` и `backend-2`.

После остановки одного экземпляра второй продолжает обслуживать запросы:

```text
backend-1 остановлен
        ↓
все запросы обрабатывает backend-2
```

Если остановлены оба backend, Nginx возвращает:

```text
502 Bad Gateway
```

## Технологии

- Python;
- Flask;
- Bash;
- Linux;
- Nginx;
- Git;

## Дальнейшее развитие

Следующие этапы проекта:

```text
Docker
Docker Compose
CI/CD
Ansible
Kubernetes
Terraform
Prometheus
Grafana
```